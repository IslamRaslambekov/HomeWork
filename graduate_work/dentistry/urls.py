from django.urls import path

# from dentistry.views import ReceptionView, main_page

urlpatterns = [
    # path('', main_page, name='home'),
    # path('reception', ReceptionView.as_view(), name='reception'),
]
